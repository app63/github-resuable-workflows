name: Calling Hello World Reusable Workflow

on:
  push:
    branches:
      - main

jobs:
  use-hello-world-workflow:
    uses: ./github/workflows/hello-world-reusable.yml
    with:
      name: "GitHub Actions"
